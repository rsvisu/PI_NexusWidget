# Declaramos el propio repo como único paquete para que pnpm acepte este archivo (lo exige pnpm 9, el de CI)
# onlyBuiltDependencies: pnpm 10+ bloquea los scripts de instalación por seguridad; permitimos el de esbuild
packages:
  - '.'
onlyBuiltDependencies:
  - esbuild
